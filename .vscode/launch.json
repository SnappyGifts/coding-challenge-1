{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "run service",
        "program": "${workspaceFolder}/node_modules/.bin/sls",
        "cwd": "${workspaceFolder}",
        "args": ["offline", "start"],
        "outputCapture": "std"
      }
    ]
  }